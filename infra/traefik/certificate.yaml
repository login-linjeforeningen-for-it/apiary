apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-login-no
  namespace: traefik
spec:
  secretName: wildcard-login-no
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
    - "login.no"
    - "*.login.no"
